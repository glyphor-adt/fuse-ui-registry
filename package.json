{
  "name": "fuse-ui-registry",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter preview dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
